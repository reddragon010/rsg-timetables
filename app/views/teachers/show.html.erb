<p id="notice"><%= notice %></p>

<h3>Subjects</h3>
<ul>
	<% @teacher.subjects.each do |subject| %>
	<li><%= subject.short %></li>
	<% end %>
</ul>

<%= render :partial => "shared/timetable", :locals => {:headline => @teacher.short,:tt_rows => @tt_rows} %>